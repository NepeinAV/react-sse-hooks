import * as React from 'react';
import { EventSourceProviderProps } from './types';
declare const EventSourceProvider: ({ eventSource, children }: React.PropsWithChildren<EventSourceProviderProps>) => React.JSX.Element;
export default EventSourceProvider;
